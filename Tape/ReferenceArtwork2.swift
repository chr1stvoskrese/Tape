import UIKit

/// Compatibility name for the physical-player renderer.
/// Keep the reference image in a single, known-good implementation.
enum ReferenceArtwork2 {
    static var image: UIImage {
        ReferenceArtwork.image
    }
}
